# 初始条件： 原始网络图，社区检测结果的社区分类

# 给定目标节点T，和需要隐藏的社区Ci，

# 步骤一：求某个社区中各个节点的重要度。
-- 社区中所有节点存储：字典dic1{标号Tj，重要度}
   - 数据源：*.dat2个文件
   - 存储源：
     - ci = [103 1 119]
     - t = 103/1/119
     - v = [119 118 117 118 ....] => [103:1 119,1:11,111,], network里面取,并且判断是否属于ci,
     - tb = {}, tvb = {},network里面取,去重,并且判断是否属于ci,
     - delCount = len(tb中b在ci的个数)-1
     - tvc = {},tvb属于ci中的元数
     - td = tb
   - 算法： Inf(T,Ci)=(节点T与邻居v之间在社区Ci中的公共元素个数)*（节点T的度-1）/节点T的度

# 步骤二：基于节点重要度的节点社区隐藏算法。
   - 数据源：network.dat 、ci、 t、tinf、tb
   - 存储源：network.dat
     - ci = [103 1 119]
     - tt = 103
     - tinf = [103:10,1:8,119:7]
     - tb: tinf中值最大,103-1,103-119,max:103-1
     - maxtv = 1，找到度最大度为1
     - network.dat删除:边103-1/1-103所有
     - network.dat增加：弥补删除的边数且边的节点非ci节点